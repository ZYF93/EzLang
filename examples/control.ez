// control.ez - 验证控制流
let x = 10;
(x > 5) ? {
    x = x + 1;
} : {
    x = x - 1;
};

loop i in 0...10 {
    (i == 5) ? break;
}

let result = 0;
match {
    (x == 10) ? { result = 1; },
    (x == 20) ? { result = 2; },
    (true) ? { result = 3; }
}
