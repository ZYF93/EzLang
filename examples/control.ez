// control.ez - 验证控制流
let x = 10;
if (x > 5) {
    x = x + 1;
} else {
    x = x - 1;
}

loop i in 0...10 {
    if (i == 5) { break; }
}

let result = match {
    x == 10 => "ten";
    x == 20 => "twenty";
    true => "other";
};
