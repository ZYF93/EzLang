// structs.ez - 验证结构体
struct Point {
    x: I32;
    y: I32 = 0;
};

struct Point3D {
    ...Point; // 字段展开
    z: I32;
};

const run = () => {
    let p1 = Point(x = 10);
    let p2 = Point3D(x = 1, y = 2, z = 3);
    return p1.x;
};
