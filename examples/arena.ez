// arena.ez - 验证内存模型
struct Data { val: I32; };

const create = () => {
    let d = Data(val = 42);
    d; // 提升到父作用域
};

const main = () => {
    let result = {
        let x = 10;
        let p = Data(val = x);
        p; // 提升到 main 作用域
    };
    create();
};
