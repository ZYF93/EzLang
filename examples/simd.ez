from "std/io" import { println };

const main = (): I32 => {
    const v1: Vec<I32>[4] = Vec[1, 2, 3, 4];
    const v2: Vec<I32>[4] = Vec[5, 6, 7, 8];
    const sum = v1 + v2;
    const scaled = sum * 2;

    println(msg = "simd: vector add and scalar broadcast compiled");
    return 0;
};
